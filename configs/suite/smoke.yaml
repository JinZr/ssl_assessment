prepare: true
config_overrides:
  model:
    max_total_sec: 45
    max_input_sec: 30
models:
  - wavlm_base
tasks:
  - sap_naturalness
pairs:
  - qs_nat_to_sap_nat
methods:
  - baseline
  - jt
  - ft
ratios:
  - 1.0
seeds:
  - 13
protocols:
  - paper_faithful
reviewer_controls: []
summarize: true
tables: true
figures: true
report: true
